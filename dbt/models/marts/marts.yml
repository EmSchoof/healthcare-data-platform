version: 2

models:
  - name: dim_patients
    description: "Analytics-ready patient dimension."
    columns:
      - name: patient_id
        tests:
          - not_null
          - unique

      - name: mrn
        tests:
          - not_null

  - name: patient_summary
    description: "Simple aggregate summary of patients by geography and gender."